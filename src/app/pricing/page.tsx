"use client";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
const plans = [["Starter",10,"$9","For a focused first search"],["Momentum",50,"$39","For an active role pipeline"],["Studio",100,"$69","For a growing recruiting team"],["Network",500,"$249","For teams hiring all year"]] as const;
export default function Pricing() { return <div className="container mx-auto px-4 py-16"><div className="mx-auto max-w-2xl text-center"><p className="font-mono text-xs uppercase tracking-widest text-primary">Simple, useful currency</p><h1 className="mt-3 text-4xl font-bold tracking-tight">Buy more context, not more noise.</h1><p className="mt-4 text-muted-foreground">Tokens are only spent when you scan a full profile. Browsing and saving are always free.</p></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{plans.map(([name, amount, price, description], i) => <Card key={name} className={i === 1 ? "border-primary shadow-md" : ""}><CardHeader><p className="text-sm text-muted-foreground">{name}</p><CardTitle className="text-3xl">{amount}<span className="ml-1 text-sm font-normal text-muted-foreground">tokens</span></CardTitle><p className="text-sm text-muted-foreground">{description}</p></CardHeader><CardContent><p className="mb-5 text-2xl font-semibold">{price}</p><Button className="w-full" variant={i === 1 ? "default" : "outline"} onClick={() => toast.info("Demo checkout", { description: "Payments are not connected in this workspace." })}>Choose {name}</Button></CardContent></Card>)}</div><p className="mx-auto mt-10 max-w-lg text-center text-xs text-muted-foreground">All plans are demo-only. Your current wallet remains unchanged.</p></div>; }
