"use client";
import { Check, Coins, Sparkles } from "lucide-react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
const plans = [["Starter", "10", "Untuk eksplorasi awal"], ["Growth", "30", "Untuk hiring yang konsisten"], ["Scale", "100", "Untuk tim yang sedang tumbuh"]];
export default function PricingPage() { return <div className="container mx-auto max-w-6xl px-4 py-14"><div className="mx-auto max-w-2xl text-center"><p className="font-mono text-xs uppercase tracking-widest text-[#19a974]">Token plans</p><h1 className="mt-3 text-4xl font-bold">Bayar hanya saat kamu butuh lebih banyak konteks.</h1><p className="mt-4 leading-7 text-muted-foreground">Setiap unlock profile menggunakan satu token. Browse dan shortlist tetap gratis.</p></div><div className="mt-10 grid gap-5 md:grid-cols-3">{plans.map(([name, tokens, copy], index) => <Card key={name} className={index === 1 ? "border-[#19a974] shadow-lg" : ""}><CardContent className="p-6"><div className="flex items-center justify-between"><p className="font-bold">{name}</p>{index === 1 && <span className="rounded-full bg-[#e3f5ed] px-2 py-1 text-xs font-semibold text-[#08744f]">Popular</span>}</div><p className="mt-6 flex items-center gap-2 font-mono text-4xl font-bold"><Coins className="size-7 text-[#19a974]" />{tokens}</p><p className="mt-2 text-sm text-muted-foreground">tokens · {copy}</p><div className="mt-6 space-y-3 text-sm text-muted-foreground"><p><Check className="mr-2 inline size-4 text-[#19a974]" />Access verified profiles</p><p><Check className="mr-2 inline size-4 text-[#19a974]" />Save notes and shortlist</p></div><Button className="mt-7 w-full" variant={index === 1 ? "default" : "outline"} onClick={() => toast.info("Demo only", { description: "Pembayaran belum terhubung. Token balance tidak berubah." })}><Sparkles className="size-4" /> Choose {name}</Button></CardContent></Card>)}</div><p className="mt-8 text-center font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Demo purchase flow · No payment mutation</p></div>; }
