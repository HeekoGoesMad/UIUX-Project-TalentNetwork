"use client";
import { useApp } from "@/providers/app-provider";
import { Button } from "@/components/ui/button";
import { ProtectedRoute } from "@/components/auth/protected-route";
export default function Page() { const { screeningConsents, respondToConsent } = useApp(); const pending = Object.entries(screeningConsents).filter(([, state]) => state === "pending-candidate-consent"); return <ProtectedRoute role="candidate"><main className="container mx-auto max-w-3xl px-4 py-8"><h1 className="text-3xl font-bold">Contact requests</h1><p className="mt-2 text-muted-foreground">Kamu memegang kendali atas permintaan screening.</p><div className="mt-8 space-y-3">{pending.length ? pending.map(([id]) => <div key={id} className="flex flex-col gap-3 rounded-2xl border bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold">Recruiter ingin meminta screening</p><p className="text-sm text-muted-foreground">Candidate ID: {id}</p></div><div className="flex gap-2"><Button size="sm" onClick={() => respondToConsent(id, "consented")}>Izinkan</Button><Button size="sm" variant="outline" onClick={() => respondToConsent(id, "declined")}>Tolak</Button></div></div>) : <div className="rounded-2xl border border-dashed p-8 text-center text-sm text-muted-foreground">Belum ada permintaan baru.</div>}</div></main></ProtectedRoute>; }
