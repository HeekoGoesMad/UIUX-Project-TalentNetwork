import { BriefcaseBusiness, UserRound } from "lucide-react";
import { UserRole } from "@/types";
export function RoleSelector({ role, onChange }: { role: UserRole; onChange: (role: UserRole) => void }) { return <div className="grid grid-cols-2 gap-3">{([ ["candidate", "Candidate", UserRound], ["recruiter", "Recruiter", BriefcaseBusiness] ] as const).map(([value, label, Icon]) => <button type="button" key={value} aria-pressed={role === value} onClick={() => onChange(value)} className={`rounded-2xl border p-4 text-left transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring ${role === value ? "border-[#19a974] bg-[#e3f5ed] ring-2 ring-[#19a974]/20" : "hover:bg-muted"}`}><Icon className="size-5 text-[#19a974]" aria-hidden="true" /><span className="mt-3 block text-sm font-semibold">{label}</span><span className="mt-1 block text-xs text-muted-foreground">{value === "candidate" ? "Showcase your journey" : "Find verified talent"}</span></button>)}</div>; }
