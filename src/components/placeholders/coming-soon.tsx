import Link from "next/link";
import { ArrowRight, Bell, BriefcaseBusiness, MessageCircle } from "lucide-react";
import { Button } from "@/components/ui/button";
export function ComingSoon({ kind }: { kind: "jobs" | "messages" }) { const jobs = kind === "jobs"; const Icon = jobs ? BriefcaseBusiness : MessageCircle; return <div className="container mx-auto flex min-h-[calc(100vh-14rem)] max-w-2xl items-center justify-center px-4 py-16"><div className="w-full rounded-3xl border bg-white p-8 text-center shadow-sm sm:p-14"><div className="mx-auto flex size-16 items-center justify-center rounded-2xl bg-[#e3f5ed] text-[#08744f]"><Icon className="size-8" /></div><p className="mt-7 font-mono text-xs uppercase tracking-widest text-[#19a974]">Coming soon</p><h1 className="mt-3 text-3xl font-bold">{jobs ? "Job discovery sedang disiapkan" : "Messages akan segera hadir"}</h1><p className="mx-auto mt-4 max-w-md leading-7 text-muted-foreground">{jobs ? "Kami sedang membangun cara yang lebih baik untuk menemukan peluang yang cocok dengan perjalananmu." : "Percakapan langsung antara talent dan recruiter akan hadir di update berikutnya."}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Button asChild><Link href={jobs ? "/profile" : "/dashboard"}>{jobs ? "Lengkapi profile" : "Kembali ke dashboard"} <ArrowRight className="size-4" /></Link></Button><Button variant="outline"><Bell className="size-4" /> Notify me</Button></div><p className="mt-6 font-mono text-[10px] uppercase tracking-widest text-muted-foreground">Demo placeholder · No backend connected</p></div></div>; }
