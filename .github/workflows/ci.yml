name: CI

on:
  push:
    branches:
      - main
      - master
      - dev
      - "milestone-*"
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      # Build requires no env secrets (guards fail closed).
      - run: npm run build
